apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-marcone.fullname" . }}
  labels:
    {{- include "app-marcone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-marcone.selectorLabels" . | nindent 4 }}
